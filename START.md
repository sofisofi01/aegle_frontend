# 🚀 Aegle Frontend - Quick Start

## 📋 Предварительные требования

- Node.js 18+ 
- npm 9+
- Backend сервер запущен на http://localhost:3000

## ⚡ Быстрый старт

### 1. Установка зависимостей
```bash
npm install
```

### 2. Настройка окружения
Создайте файл `.env.local`:
```env
NEXT_PUBLIC_API_URL=http://localhost:3000
NEXT_PUBLIC_APP_URL=http://localhost:3001
```

### 3. Запуск разработки
```bash
# Обычный запуск
npm run dev

# Если есть проблемы с Turbopack:
npm run dev -- --turbo=false
```

### 4. Откройте в браузере
```
http://localhost:3001
```

## 🛠️ Доступные команды

```bash
# Разработка
npm run dev           # Development сервер
npm run build         # Production сборка
npm run start         # Production сервер
npm run lint          # Проверка кодстайла

# Анализ
npm run analyze       # Анализ bundle размера
```

## 🏗️ Структура проекта

```
src/
├── app/                 # App Router pages
│   ├── (auth)/         # Auth группы роутов
│   ├── (dashboard)/    # Dashboard группы роутов  
│   └── globals.css     # Глобальные стили
├── components/          # React компоненты
│   ├── ui/             # Базовые UI компоненты
│   ├── auth/           # Компоненты аутентификации
│   ├── dashboard/      # Компоненты дашборда
│   ├── nutrition/      # Компоненты питания
│   ├── workouts/       # Компоненты тренировок
│   ├── progress/       # Компоненты прогресса
│   └── ai-coach/       # Компоненты ИИ-ассистента
├── lib/                # Утилиты и конфигурации
├── store/              # Redux store
├── types/              # TypeScript типы
├── hooks/              # Custom React hooks
└── utils/              # Вспомогательные функции
```


## 📞 Поддержка

При возникновении проблем:
1. Проверьте что все зависимости установлены
2. Убедитесь что бэкенд запущен
3. Проверьте настройки в `.env.local`

---

**Готовы к разработке! 🎉**